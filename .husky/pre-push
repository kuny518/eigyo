# main への直接 push はブランチ保護で拒否されるが、送信前に気付けるようにローカルでも止める
while read -r _local_ref _local_sha remote_ref _remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "main への直接 push はできません。ブランチを切って PR を作成してください。" >&2
    exit 1
  fi
done

npm run lint
npm run typecheck
npm test
